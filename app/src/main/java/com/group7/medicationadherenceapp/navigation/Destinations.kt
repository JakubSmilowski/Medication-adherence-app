package com.group7.medicationadherenceapp.navigation

object Dest {
    // existing routes your project already uses
    const val LOGIN = "login"
    const val HOME = "home"

    const val REGISTER = "register"

    // caregiver feature routes (bottom bar + screen)
    const val CAREGIVER = "caregiver"
    const val HISTORY = "history"
    const val PROFILE = "profile"
    const val SETTINGS = "settings"

    // details (keep as your project currently uses it)
    const val MED_DETAILS = "medicationDetails/{medName}"
}